# CLI-Student-Manager
A Command Line Interface (CLI) based Student Management System built using Python. This project helps manage student records such as adding, viewing, updating, deleting, and storing data persistently using files.
